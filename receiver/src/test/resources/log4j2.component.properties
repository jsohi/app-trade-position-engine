# Use LMAX Disruptor ring buffer for all loggers — log calls on the hot path return immediately
# without allocating and without blocking on I/O.
log4j2.contextSelector=org.apache.logging.log4j.core.async.AsyncLoggerContextSelector

# GC-free encoding: reuse pre-allocated byte buffers when formatting and writing log events.
# Default since Log4j2 2.6, stated explicitly here for clarity.
log4j2.enableDirectEncoders=true
